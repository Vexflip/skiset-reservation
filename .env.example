DATABASE_URL="file:./dev.db"

# SMTP Configuration for Emails (Gmail, Resend, SendGrid, etc.)
# For Gmail: smtp.gmail.com, port 587, and use an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
